import { RippleEffect } from './ripple.directive';

describe('RippleEffect', () => {
  it('should create an instance', () => {
    const directive = new RippleEffect();
    expect(directive).toBeTruthy();
  });
});
