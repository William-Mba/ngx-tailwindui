import { ExtendedFabDirective } from './extended-fab.directive';

describe('ExtendedFabDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtendedFabDirective();
    expect(directive).toBeTruthy();
  });
});
